import 'package:flutter/material.dart';

class AppColors {
  // Dark Mode
  static const Color primary = Color(0xFF2e84ef);

  static const Color background = Color(0xFF0b0f17);
  static const Color surface = Color(0xFF14171f);
  static const Color surfaceBorder = Color(0xFF292c33);

  static const Color textPrimary = Color(0xFFEDF1F4);
  static const Color textSecondary = Color(0xFF939CA6);
  static final Color textHint = Colors.white.withValues(alpha: 0.036);

  static const Color divider = Color(0xFF21252c);

  static const Color error = Color(0xFFf14d4c);
  static const Color success = Color(0xFF00bc7d);
  static const Color warning = Color(0xFFFFC107);

  // static const Color secondary = Color(
  //   0xFF0087EF,
  // ); // Accent/complement Badges, toggles, highlights

  // static const Color onPrimary = Color(
  //   0xFF939CA6,
  // ); // Text/icon ON primary bgText inside a filled button

  // static const Color onSurface = Color(
  //   0xFF939CA6,
  // ); // Text/icon ON surface bgText inside cards
}
